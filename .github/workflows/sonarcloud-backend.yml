name: SonarCloud Backend

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

jobs:
  sonarcloud:
    name: Build and analyze backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Build services and generate coverage
        shell: bash
        run: |
          services=(
            auth-service
            template-service
            resume-service
            section-service
            api-gateway
            ai-service
            export-service
            notification-service
            jobmatch-service
            eureka-server
          )

          for service in "${services[@]}"; do
            echo "Building $service"
            (cd "$service" && mvn -B org.jacoco:jacoco-maven-plugin:0.8.12:prepare-agent test org.jacoco:jacoco-maven-plugin:0.8.12:report)
          done

      - name: SonarCloud scan
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
